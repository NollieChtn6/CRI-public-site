---
import Layout from '../../layouts/Layout.astro';
import PartnerCard from '@/components/PartnerCard.astro';
import partners from '../../data/partners.json';
---

<Layout>
  <div class="max-w-5xl mx-auto px-4 py-12 space-y-12 text-shark-50">
    <header class="text-center space-y-4">
      <h1 class="text-4xl font-bold text-red-orange-500">Nos Partenaires</h1>
      <p class="text-shark-200 text-lg sm:text-xl">
        Code Rouge Info collabore avec de nombreux acteurs de la Sécurité Civile
        et de la Défense <br />pour valoriser leur travail et promouvoir leurs
        actions auprès du grand public.
      </p>
    </header>

    <section>
      <div
        class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-6 justify-center items-stretch"
      >
        {
          partners.map((partner) => (
            <PartnerCard
              name={partner.name}
              website={partner.website}
              logo={partner.logo}
            />
          ))
        }
      </div>
    </section>
  </div>
</Layout>
