---
import ImageWithPlaceholder from '@/atoms/ImageWithPlaceholder.astro';

export interface PartnerCardProps {
  name: string;
  website: string | null;
  logo: string;
}

const { name, website, logo } = Astro.props as PartnerCardProps;
---

<article
  class="partner-card flex flex-col items-center text-center p-4 bg-shark-900 rounded-xl shadow-md shadow-black/40 hover:shadow-lg transition-shadow justify-between"
>
  <ImageWithPlaceholder src={logo} alt={name} class="object-contain mb-2" />

  {
    website ? (
      <a
        href={website}
        target="_blank"
        rel="noopener noreferrer"
        class="block"
        title={`Consulter le site du partenaire : ${website}`}
      >
        <span class="text-sm font-medium text-shark-50 hover:text-dark-blue-400 transition-colors">
          {name}
        </span>
      </a>
    ) : (
      <p class="text-sm font-medium text-shark-50">{name}</p>
    )
  }
</article>
