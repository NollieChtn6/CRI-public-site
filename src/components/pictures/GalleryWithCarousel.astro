---
import type { Picture } from '@/@types/types';
import GalleryClient from './GalleryClient.tsx';

const { pictures } = Astro.props as { pictures: Picture[] };
---

<GalleryClient pictures={pictures} client:load />
