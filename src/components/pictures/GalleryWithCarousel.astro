---
import type { Picture } from '@/@types/types';
import GalleryClient from './GalleryClient.tsx';

const { pictures, type } = Astro.props as {
  pictures: Picture[];
  type: 'gallery' | 'article';
};
---

<GalleryClient pictures={pictures} type={type} client:load />
