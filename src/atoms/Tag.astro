---
const { label, url } = Astro.props as { label: string; url: string };
---

<a href={url} class="" title={`Aller à la galerie : ${label}`}>
  <span
    class="inline-block px-2 py-1 bg-gray-200/70 rounded uppercase text-xs font-semibold hover:scale-105 transition-transform duration-400 ease-in-out"
    >#{label}</span
  ></a
>
